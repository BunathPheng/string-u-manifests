---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: string-u                  # Ingress name
  labels:
    app: string-u
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "string-u.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: string-u               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
